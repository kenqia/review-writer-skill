# Chemical Literature Review Skill

本项目定义一组面向化学研究者、可独立调用且可组合的四阶段 skills，用显式文档交接与可追溯证据支持批判性叙述综述的持续迭代。旧的七阶段 runtime 仅作为迁移期 legacy，不是新的用户入口或领域流程。

## Canonical Four-Stage Flow

**综述意图（Intent）**：把主题整理为研究问题、核心论点、读者、范围边界、预期贡献和排除项，并形成 `review-brief.md`。

**研究证据阶段（Research）**：围绕已确认的综述意图发现文献、获取合法全文、解析来源并整理 locator-bound evidence；阶段产物通过 `research/` 文档目录交接，不依赖中央 orchestrator。

**综合阶段（Synthesis）**：直接读取综述意图和研究证据，生成带比较、解释、反驳、假设与 Chemical GAP 的候选正文；它是正文 producer，不要求先构造软件工程 payload。

**QA 审查阶段（QA）**：可选地组织多代理、干净上下文的独立审查，并为人类生成 QA 计划、审查报告和修订路线；它不替人类作最终科学接受决定。

**显式文档交接**：相邻 skill 通过约定的 Markdown 产物交换上下文；文件是阶段边界，旧 runtime 的 Python 状态对象不是 canonical 产品接口。

**多代理独立审查**：固定的证据与 locator、化学可比性与机制、综合论点与反驳、过度主张与反例四类审查 agent，在互相隔离、无前一审查结论污染的上下文中检查同一候选稿；允许按项目增加角色，但不得省略基础角度。

**审查汇总 agent**：读取同一版正文、研究证据和所有独立审查报告，保留真实冲突并将问题路由到 Intent、Research、Synthesis 或表达/交付修订；它不通过投票替人类作科学接受决定。

**未审查候选稿**：尚未经过 QA 或人类科学接受的 Synthesis 输出；它可以继续迭代，但不得被描述为科学有效、正式投稿包或已获人类认可。

**Legacy v1 runtime**：旧的七阶段状态机、orchestrator 和内部 payload 集合；不再作为可安装或受支持的产品路线，迁移完成后允许删除，不得成为 v2 skill 的隐式依赖。

**推荐全文路线**：Research 必须提供可配置且可运行的元数据发现、合法全文定位/获取和解析接口；provider 未配置、全文受限或不能自动下载时，必须给出合法下载地址、授权说明、指定放置目录和下一步等待动作，而不是伪造已获得全文。

**用户下载等待**：用户从 Research 提供的合法地址自行下载核心论文并放入指定授权目录后，Research 从保存的交接资产继续执行全文登记和解析；等待用户动作不等于研究失败，也不允许跨过证据准备边界。

**证据可用性记录**：不新增一组独立的来源状态枚举；全文是否存在、访问依据、下载地址、放置路径、解析 locator 和缺口写在来源登记与交接文档中，用明确文字表达，不扩张主流程状态机。

**完整 provider adapter 路线**：v2 必须实现并测试 OpenAlex、Semantic Scholar、Crossref、PubChem、ChEBI、Unpaywall、Europe PMC 和 MinerU 的可配置接口；名称列表或协议声明不算完成。未配置凭据或不可用 provider 必须报告真实降级，不得伪装可用。

**凭据边界**：provider 配置可通过环境变量或用户本地、未纳入版本控制的 env 文件提供；仓库只提供变量名和 `.env.example`，真实 API key、token、cookie 和 session 不写入 repo、交接文档或 Research cache。

**解析器路线**：MinerU 是 v2 的正式主解析器；`pdftotext` 只作为本地授权 PDF 的低保真 fallback。GROBID 和 Docling 不属于 v2 parser 实现。解析文本始终是 locator-bound reading aid，原始 PDF 才是来源权威。

**合法全文双路获取**：公开、直接且授权明确的 PDF URL 可以由 Research 自动下载；需要登录、机构权限、验证码、复杂网页交互或授权不明确的来源只提供合法下载地址并等待用户，不绕过访问控制。自动下载要保留来源 URL、访问依据、下载时间和文件 digest。

**授权 PDF inbox**：用户下载的论文进入项目 `research/inbox/authorized-pdfs/`；`research/download-requests.md` 记录每篇论文的稳定 identity、合法 URL、访问依据、建议文件名、核心论点关联和下一动作。无法通过 DOI、文件名、用户确认或 manifest 可靠绑定时必须请求人工确认。

**Research 阶段结果**：Research 不增加来源级状态机，只返回 `READY_FOR_SYNTHESIS`、`WAITING_FOR_USER` 或 `RESEARCH_GAP` 三种阶段结果；详细全文存在性、访问依据、解析 locator 和缺口写入 source registry 与交接文档。

**优先全文队列**：核心全文需求按论点相关性和不可替代性决定，不按固定论文数量决定；Research 必须说明每篇缺失全文会影响哪条核心论点，并输出有限的优先下载队列。

**证据边界内综合**：`RESEARCH_GAP` 时 Synthesis 可以生成已有全文证据支持的局部候选稿，但必须显式保留 Chemical GAP、待验证说明和模型假设，不得补写缺失的 SOURCE_FACT 或冒充完整综述。

**Delivery skill**：独立于四阶段核心流程、按需读取 `draft.md` 和研究证据的交付能力，例如 DOCX、Figure/Scheme/Table 或 Journal Profile；它不拥有第二份正文权威。

**Issue supersession**：旧 v1 Issue 不再继续实现时，优先关闭并在关闭说明中指向 v2 设计；不删除有历史价值的 Issue，也不让它们继续进入 `ready-for-agent` 执行队列。

## Review Artifacts

**批判性叙述综述**：围绕明确的化学问题组织和比较文献，不只汇总结果，还解释方法差异、证据矛盾、适用边界与研究空白。

**投稿候选包**：可供研究者继续修改和人工把关的综述交付物，至少包括正文草稿、来源与证据绑定、覆盖/缺口信息、未解决的不确定项，以及与目标期刊的适配说明；它不等同于期刊接收。

**迭代综述闭环**：QA agent 或人类科学编辑发现问题后，将反馈路由回最早失效的 Intent、Research 或 Synthesis 阶段，并从该阶段继续后续工作；它不是一次性完成的线性流水线。

**项目领域画像**：单个综述项目对化学子领域、核心体系、术语、范围边界和适用证据标准的明确描述；它限定本项目的研究语境，不是覆盖全部化学的通用知识库。

**研究证据包**：Research 阶段为当前综述积累的来源、定义、关键事件、比较线索和待核实判断的可迭代材料集合；它帮助模型思考和写作，不要求一开始就被固定成完整数据库或穷尽所有缺口。

**轻量证据护栏**：只对伪造来源、错引原文、捏造具体数据或把未经支持的化学判断写成确定事实设置硬约束；其余追踪信息、矛盾和缺口优先作为提示与复核线索，不应阻断模型的综合、假设和观点生成。

**模型研究同伴**：在研究者设定的问题和证据范围内，能够主动比较文献、提出反驳、形成综合判断和构造可检验假设的模型角色；它可以挑战研究者的初始看法，但不因此获得期刊或科学共同体的最终裁决权。

**主张层级**：对综述中的关键判断区分来源事实、模型综合和模型假设三种表达层级；区分是为了让人类审阅和后续迭代知道判断的性质，不是为了压制模型推理。

**双轨综述交付**：同一轮综述内容的两个视图：面向读者和投稿的干净正文，以及面向研究者核验的带主张层级和依据标记的研究版；两者必须保持内容同步，不能各自独立演化。

**综述意图契约**：对当前项目的研究问题、核心论点、目标读者或期刊、范围边界、预期贡献和明确排除项的可迭代约定；改变核心意图需要研究者明确确认。

**人类科学编辑**：对综述的科学立场、核心论点和最终交付文本拥有最终接受、修改或拒绝权的研究者角色；该角色不等于逐阶段替模型完成所有工作。

**单一综述内容源**：承载当前综述论点、段落和研究判断的唯一可迭代内容基线；干净稿与研究版均由它生成，避免两个版本独立漂移。

**能力优先研究路线**：Research 按当前可用能力选择文献发现、来源获取、解析和综合工具；必要时请求研究者完成最小配置，工具不可用时允许降级并说明受影响的研究能力。

**自适应多路径检索**：围绕同一综述意图并行展开术语、方法/材料、关键事件、引用关系和最新进展等检索路径，并依据新信息对论点和下一步搜索进行调整；它不以固定检索式或固定论文数量定义完成。

**分层文献集**：按对当前综述论点的作用组织锚点/核心论文、扩展论文、背景/定义资料和争议论文的候选集合；层级可以在 Research、Synthesis 和 QA 中被提升、降级或删除。

**研究交接判断**：Research 对当前材料是否足以支持下一步 Synthesis 或 QA 的临时建议；它必须说明已覆盖方向、未覆盖的高影响区域、全文获取缺口和主要不确定性，不能只报告一个数量。

**多代理 QA 审查**：由多个隔离上下文的审查 agent 分别检查证据绑定、化学可比性、论证与反驳质量、主张层级和交付风险，再形成面向人类的 QA 计划；它不能替代人类科学接受。

**自然语言反馈回路**：研究者可用直接改稿、评论或普通语言表达 QA 或科学编辑意见，agent 将其解释为可执行反馈并路由到最早失效阶段，而不是要求研究者填内部表单。

**价值假设**：关于这篇综述为什么值得现在被写、会改变什么理解或研究决策的可争辩主张；它可以由模型提出多个候选，但必须在 Research、Synthesis 和 QA 中持续接受证据与反例挑战。

**期刊适配**：根据研究者确认的目标期刊及其当前官方要求调整综述的结构、篇幅、图表、引用和表达格式；它影响投稿准备度，不等同于科学有效性或接收预测。
