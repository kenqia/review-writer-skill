# 泛化的 Chemical Framework 模型

Status: accepted

Chemical Framework 不采用催化、材料或药化中的任何单一固定 schema，而采用“通用比较主轴 + confirmed brief 驱动的领域模块 + 解释链/判断框架”。这样可以在不同化学主题间复用比较和证据推理方法，同时避免把不适用的字段强行套入项目；当研究没有共同终点时输出证据地图和不可比较边界，不强行排名；Framework 的默认价值检查和改变判断的案例选择也归入这一层，且不以固定数量作为完成门槛。
