# Candidate return branch

Researcher chose `return_to_discovery` because the mechanism path had insufficient coverage. The previous candidate set and reasons remain; no new full-text queue was created until a later acceptance.
