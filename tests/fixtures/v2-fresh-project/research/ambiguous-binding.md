# Ambiguous PDF binding branch

User PDF matched two candidate identities and version metadata conflicted. Binding was held as `UNKNOWN identity` / `WAITING_FOR_USER`; no evidence fact was promoted until explicit selection.
